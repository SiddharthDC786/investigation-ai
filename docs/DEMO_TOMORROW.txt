#!/usr/bin/env bash
# Vigil SIH PS 26189 — judge demo startup (5-minute checklist)
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"

cat <<'EOF'
╔══════════════════════════════════════════════════════════════╗
║  VIGIL DEMO — Smart India Hackathon PS 26189                 ║
╚══════════════════════════════════════════════════════════════╝

BEFORE JUDGES ARRIVE (once)
───────────────────────────
Option A — Local (fastest for laptop demo)
  1. Postgres.app → database: crime_network (seeded)
  2. ./scripts/start-vigil.sh  (auto-starts Neo4j + spaCy backend)
  3. Open http://localhost:5173

Option B — Docker full stack
  cd backend-api && docker compose up -d --build
  docker compose --profile seed up seed   # first time only
  cd ../frontend && npm run dev

LOGIN
─────
Investigator: INV-2847 / vigil2026
Supervisor:   SUP-1001 / admin2026  (audit export)

5-MINUTE DEMO SCRIPT
────────────────────
0. BADGE — show "Live backend · Neo4j fusion · NLP: spacy"
1. AI INGEST — right panel → Load demo FIR → Preview → Ingest (spaCy + Neo4j)
2. SEARCH — tap "Same name collision" → pick Mumbai Rahul Mukherjee
3. NETWORK — burner bridge (prepaid SIM linking suspects)
4. TIMELINE — CDR + transaction fusion events
5. RISK — composite triage + community clusters
6. OSINT — lookup → hash-chained audit
7. INSPECTOR — Explainability (court-defensible narrative)
8. AUDIT — supervisor exports disclosure JSON

HEALTH CHECKS
─────────────
  curl http://localhost:8000/health
  → postgres, neo4j, nlp.engine (spacy or regex)

NEO4J BROWSER (optional wow factor)
  http://localhost:7474  neo4j / vigilneo4j2026

TROUBLESHOOTING
───────────────
• Backend offline → port 8000 or frontend/.env
• NLP shows "regex" → run: python -m spacy download en_core_web_sm
• Neo4j optional — PostgreSQL fallback keeps demo working

STOP
────
Ctrl+C in start-vigil.sh terminal
EOF
