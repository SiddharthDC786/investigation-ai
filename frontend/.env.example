# Friend's FastAPI base URL (no trailing slash)
# Same WiFi example — replace with friend's laptop IP:
# VITE_API_BASE_URL=http://192.168.1.42:8000
#
# If friend uses ngrok:
# VITE_API_BASE_URL=https://abc123.ngrok-free.app
#
# Leave empty to use mock data only (no backend):
VITE_API_BASE_URL=
